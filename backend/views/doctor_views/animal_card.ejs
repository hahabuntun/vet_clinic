<!-- На этой странице врач смотрит/создает кароточку животного(все приемы и анализы) -->
<!-- На этой странице врач добавляет результаты тестов к карточке животного -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Страница электронной карты</title>
    <link rel="stylesheet" href="/styles/css/animal_card.css">
    <link rel="stylesheet" href="/styles/css/basic_nav.css">
</head>
<body>
    <header>
        <!-- Navbar -->
        <nav>
            <ul>
                <li><a href="view_appointments.html">Приемы</a></li>
                <li><a href="animal_card.html">Поиск животного</a></li>
                <li><a href="booking_schedule.html">Расписание записей</a></li>
                <li><a href="#">Выход</a></li>
            </ul>
        </nav>
    </header>
    <div id="page-container">
        <h2>Введите данные клиента</h2>
        <div id="animal-search">
            <input type="text" id="emailSearch" placeholder="Email">
            <input type="text" id="passportSearch" placeholder="Серия и номер пасспорта">
            <input type="text" id="phoneSearch" placeholder="Телефон">
            <button id="searchButton">Поиск</button>
        </div>

        <hr>

        <h2>Введите данные животного</h2>
        <div id="animal-search">
            <input type="text" id="nameSearch" placeholder="Название">
            <input type="text" id="breedSearch" placeholder="Порода">
            <input type="text" id="nicknameSearch" placeholder="Кличка">
            <input type="text" id="passportSearch" placeholder="Пасспорт животного">
            <button id="searchButton">Поиск</button>
        </div>



        <label for="pet">Выберите питомца:</label>
        <select id="pet" onchange="searchPet(this)">
            <option value="pet1">Pet 1</option>
            <option value="pet2">Pet 2</option>
        </select>


        <hr>

        <h1>
            Данные о приемах
        </h1>
        <ul id="appointmentsList">
            <!-- Здесь будут отображаться данные о приемах -->
        </ul>
        <hr>
    
        <h1>Тесты</h1>
    
        
    
        <div id="petDetails" style="display: none;">
            <h2>Результаты анализов для питомца:</h2>
            <div id="analysisResults">
                <!-- Здесь будут отображаться результаты анализов -->
            </div>
            <input type="file" id="analysisFile">
            <button onclick="uploadAnalysis()">Прикрепить файл с результатами</button>
        </div>
    </div>
    

    <script>
                // Mock data for pet analysis results
        const analysisResults = [
            { name: "ЭКГ", date: "24.02.2022" },
            { name: "Анализ крови", date: "15.03.2022" }
        ];

        // Function to search for pet by ID
        function searchPet(selectElement) {
            const selectedValue = selectElement.value;
            // Code to fetch pet details from server based on ID can be added here

            // Mocking pet data retrieval
            if (1 == 1) {
                displayPetTests();
                displayPetAppointments();
            } else {
                alert("Питомец не найден");
            }
        }

        // Function to display pet details and analysis results
        function displayPetTests() {
            document.getElementById("petDetails").style.display = "block";
            const analysisResultsContainer = document.getElementById("analysisResults");
            analysisResultsContainer.innerHTML = "";

            analysisResults.forEach(result => {
                const resultElement = document.createElement("p");
                const hrElement = document.createElement("hr");
                resultElement.textContent = `${result.name} ${result.date}`;
                analysisResultsContainer.appendChild(resultElement);
                analysisResultsContainer.appendChild(hrElement)
            });
        }

        // Function to upload analysis file
        function uploadAnalysis() {
            const file = document.getElementById("analysisFile").files[0];
            // Code to send file to server for storage can be added here
            alert("Файл с результатами отправлен на сервер");
        }



        const appointments = [
            { date: "10.02.2022", service: "Общий осмотр" },
            { date: "15.03.2022", service: "Прививка" }
        ];

        // Function to display pet appointments
        function displayPetAppointments() {
            const appointmentsList = document.getElementById("appointmentsList");
            appointmentsList.display = 'block';
            appointmentsList.innerHTML = "";
            console.log(1);
            appointments.forEach((appointment, index) => {
                
                const listItem = document.createElement("li");
                listItem.innerHTML = `
                    <p>Дата: ${appointment.date}</p>
                    <p>Услуга: ${appointment.service}</p>
                    <button onclick="viewMore(${index})">Смотреть больше</button>
                    <hr>
                `;
                appointmentsList.appendChild(listItem);
            });
        }

        // Function to redirect to details page on "Смотреть больше" button click
        function viewMore(index) {
            // Redirect to another page, passing appointment details if needed
            window.location.href = "single_appointment.html";
        }
    </script>
</body>
</html>